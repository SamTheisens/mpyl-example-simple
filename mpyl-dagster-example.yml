ops:
  report_results:
    config:
      simulate_deploy: false
execution:
  config:
    multiprocess:
      max_concurrent: 4
      start_method:
        forkserver: {}
loggers:
  console:
    config:
      log_level: WARNING
